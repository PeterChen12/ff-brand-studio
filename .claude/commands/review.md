---
description: Review the most recent IN_PROGRESS step against acceptance criteria
---

Find the step in `plans/active-plan.md` marked `IN_PROGRESS`. For that step:

1. Re-read the acceptance criteria verbatim.
2. Re-run the test command and paste the full output.
3. List files changed: run `git status` and `git diff --stat`.
4. For each acceptance criterion, state PASS or FAIL with a one-line reason.
5. If everything passes, say: "Ready to /commit."
6. If anything fails, describe the minimal fix and ask if I want you to apply it.

Do not modify any files during this review unless I explicitly ask.
